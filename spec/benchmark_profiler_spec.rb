require 'spec_helper'

describe BenchmarkProfiler do
  it 'has a version number' do
    expect(BenchmarkProfiler::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
